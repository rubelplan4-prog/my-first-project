    <style>
        :root { 
            --primary: #00bcd4; 
            --sidebar-bg: #f4f7fe; 
            --sidebar-text: #333;
            --card-shadow: 0 10px 30px rgba(0,0,0,0.1); 
            --active-tab: #00bcd4; 
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', Arial, sans-serif; }
        body { background-color: #f4f7fe; display: flex; height: 100vh; overflow: hidden; }

        /* Sidebar & Layout */
        .sidebar { width: 250px; background: var(--sidebar-bg); padding: 25px 15px; color: var(--sidebar-text); display: flex; flex-direction: column; transition: 0.3s; z-index: 100; border-right: 1px solid #d0d7de; }
        .sidebar.collapsed { margin-left: -250px; }
        .sidebar-title { font-size: 20px; font-weight: bold; margin-bottom: 40px; border-left: 5px solid var(--primary); padding-left: 10px; color: #000; }
        .nav-item { padding: 12px 15px; border-radius: 10px; cursor: pointer; color: #555; margin-bottom: 8px; transition: 0.3s; display: flex; align-items: center; gap: 10px; }
        .nav-item.active { background: #fff; color: var(--primary); font-weight: bold; box-shadow: 0 2px 5px rgba(0,0,0,0.05); }

        .main-content { flex: 1; display: flex; flex-direction: column; overflow: hidden; position: relative; }
        .header { display: flex; justify-content: space-between; align-items: center; background: #f4f7fe; padding: 15px 25px; border-bottom: 1px solid #e0e0e0; }
        .menu-btn { font-size: 24px; cursor: pointer; background: none; border: none; margin-right: 15px; }

        /* Card Styles */
        .card { width: 4in; height: 2in; background: white; border-radius: 20px; padding: 15px 25px; display: flex; justify-content: space-between; align-items: center; box-shadow: 10px 10px 20px rgba(0,0,0,0.1); border: 1px solid #eee; cursor: pointer; flex-shrink: 0; position: relative; animation: slideUp 0.6s ease-out; }

        @keyframes slideUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .three-dot { position: absolute; top: 15px; right: 15px; cursor: pointer; font-size: 20px; color: #888; padding: 5px 10px; z-index: 10; }
        .dot-menu { position: absolute; top: 45px; right: 15px; background: white; box-shadow: 0 5px 15px rgba(0,0,0,0.2); border-radius: 10px; display: none; z-index: 100; overflow: hidden; border: 1px solid #eee; }
        .dot-menu p { padding: 10px 20px; margin: 0; cursor: pointer; transition: 0.2s; font-size: 14px; white-space: nowrap; }
        .dot-menu p:hover { background: #f4f7fe; color: var(--primary); }

        .card-info h2 { font-size: 24px; color: #000; margin-bottom: 5px; font-weight: bold; padding-right: 20px; }
        .stat-row { display: grid; grid-template-columns: 80px 20px 1fr; font-size: 24px; color: #000; align-items: center; }
        .colon { color: #555; font-weight: bold; text-align: Top; }

        .circle-box { position: relative; width: 110px; height: 110px; flex-shrink: 0; }
        .circle-box svg { width: 110px; height: 110px; transform: rotate(-90deg); }
        .circle-box circle { fill: none; stroke-width: 10; stroke-linecap: round; transition: stroke-dashoffset 1.5s cubic-bezier(0.4, 0, 0.2, 1); }
        .c-bg { stroke: #f0f4f9; }
        .c-val { stroke: var(--primary); }
        .perc { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); font-size: 24px; font-weight: bold; }

        .pie-container { width: 120px; height: 120px; position: relative; }

        /* 20 Unique Card Colors & Circle Strokes */
        .card:nth-child(20n+1) { border-left: 10px solid #00bcd4; } .card:nth-child(20n+1) .c-val { stroke: #00bcd4; }
        .card:nth-child(20n+2) { border-left: 10px solid #ff9800; } .card:nth-child(20n+2) .c-val { stroke: #ff9800; }
        .card:nth-child(20n+3) { border-left: 10px solid #e91e63; } .card:nth-child(20n+3) .c-val { stroke: #e91e63; }
        .card:nth-child(20n+4) { border-left: 10px solid #4caf50; } .card:nth-child(20n+4) .c-val { stroke: #4caf50; }
        .card:nth-child(20n+5) { border-left: 10px solid #9c27b0; } .card:nth-child(20n+5) .c-val { stroke: #9c27b0; }
        .card:nth-child(20n+6) { border-left: 10px solid #3f51b5; } .card:nth-child(20n+6) .c-val { stroke: #3f51b5; }
        .card:nth-child(20n+7) { border-left: 10px solid #f44336; } .card:nth-child(20n+7) .c-val { stroke: #f44336; }
        .card:nth-child(20n+8) { border-left: 10px solid #795548; } .card:nth-child(20n+8) .c-val { stroke: #795548; }
        .card:nth-child(20n+9) { border-left: 10px solid #607d8b; } .card:nth-child(20n+9) .c-val { stroke: #607d8b; }
        .card:nth-child(20n+10) { border-left: 10px solid #ff5722; } .card:nth-child(20n+10) .c-val { stroke: #ff5722; }
        .card:nth-child(20n+11) { border-left: 10px solid #009688; } .card:nth-child(20n+11) .c-val { stroke: #009688; }
        .card:nth-child(20n+12) { border-left: 10px solid #ffc107; } .card:nth-child(20n+12) .c-val { stroke: #ffc107; }
        .card:nth-child(20n+13) { border-left: 10px solid #8bc34a; } .card:nth-child(20n+13) .c-val { stroke: #8bc34a; }
        .card:nth-child(20n+14) { border-left: 10px solid #03a9f4; } .card:nth-child(20n+14) .c-val { stroke: #03a9f4; }
        .card:nth-child(20n+15) { border-left: 10px solid #673ab7; } .card:nth-child(20n+15) .c-val { stroke: #673ab7; }
        .card:nth-child(20n+16) { border-left: 10px solid #ffeb3b; } .card:nth-child(20n+16) .c-val { stroke: #ffeb3b; }
        .card:nth-child(20n+17) { border-left: 10px solid #cddc39; } .card:nth-child(20n+17) .c-val { stroke: #cddc39; }
        .card:nth-child(20n+18) { border-left: 10px solid #ff5252; } .card:nth-child(20n+18) .c-val { stroke: #ff5252; }
        .card:nth-child(20n+19) { border-left: 10px solid #448aff; } .card:nth-child(20n+19) .c-val { stroke: #448aff; }
        .card:nth-child(20n+20) { border-left: 10px solid #e040fb; } .card:nth-child(20n+20) .c-val { stroke: #e040fb; }

        .group-card { animation: fadeIn 0.5s ease-in; transition: transform 0.3s ease; }
        .group-card:hover { transform: scale(1.02); }

        #reportView { height: 100%; padding: 25px; display: flex; gap: 25px; overflow: hidden; }
        .report-left { flex: 1.8; display: flex; flex-direction: column; gap: 25px; overflow-y: auto; }
        .report-right { flex: 1.2; display: flex; flex-direction: column; gap: 20px; overflow-y: auto; padding-right: 5px; }

        .chart-container { background: white; padding: 40px 25px; border-radius: 25px; min-height: 480px; box-shadow: var(--card-shadow); margin-top: 10px; }

        #loadingSpinner { display: none; position: absolute; inset: 0; background: rgba(255,255,255,0.7); z-index: 9999; justify-content: center; align-items: center; }
        .spinner { width: 50px; height: 50px; border: 5px solid #f3f3f3; border-top: 5px solid var(--primary); border-radius: 50%; animation: spin 1s linear infinite; }
        @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
        .hidden { display: none !important; }

        /* Modals */
        .modal { display: none; position: fixed; z-index: 1000; inset: 0; background: rgba(0,31,63,0.4); backdrop-filter: blur(5px); }
        .modal-content { background: white; margin: 2% auto; padding: 25px; width: 95%; max-width: 450px; border-radius: 20px; max-height: 90vh; overflow-y: auto; position: relative; }
        .close-icon { position: absolute; top: 15px; right: 20px; font-size: 24px; cursor: pointer; color: #888; font-weight: bold; }
        .val-in { width: 90px; padding: 8px; border: 1px solid #ddd; border-radius: 8px; text-align: right; font-weight: bold; }
        .save-btn { width: 100%; padding: 12px; background: var(--primary) !important; color: #fff; border: none; border-radius: 10px; font-weight: bold; cursor: pointer; margin-top: 15px; }

        .share-item { display: flex; justify-content: space-between; padding: 10px; border-bottom: 1px solid #eee; }

        /* User Profile Wrapper */
        .user-profile-chip { display: flex; align-items: center; background: #ffffff; padding: 5px 15px 5px 6px; border-radius: 50px; border: 1px solid #e0e6ed; cursor: pointer; transition: all 0.2s ease; box-shadow: 0 2px 8px rgba(0,0,0,0.04); }
        .user-profile-chip:hover { background: #fff5f5; border-color: #ffcdd2; transform: translateY(-1px); }
        .user-profile-chip img { width: 36px; height: 36px; border-radius: 50%; object-fit: cover; border: 2px solid var(--primary); }
        .user-profile-chip .user-info-text { display: flex; flex-direction: column; margin-left: 10px; pointer-events: none; }
        .u-name-text { font-size: 13px; font-weight: 700; color: #333; line-height: 1.2; }
        .u-email-text { font-size: 11px; color: #888; }
        .user-profile-chip:active { transform: scale(0.96); }

        .a4-container { width: 210mm; margin: 0 auto; background: #fff; color: #000; }
        .a4-page { min-height: 297mm; padding: 15mm; box-sizing: border-box; display: flex; flex-direction: column; font-family: Arial, sans-serif; }
        .report-table { width: 100%; border-collapse: collapse; border: 1px solid #000; margin-top: 20px; }
        .report-table th, .report-table td { border: 1px solid #000; padding: 8px; font-size: 18px; text-align: center; }
        #companyName { font-size: 32px; font-weight: normal; margin-bottom: 8px; text-align: center; }
        #reportTitle { font-size: 20px; font-weight: normal; margin-bottom: 10px; text-align: center; }
        #masterModeForm { display: none; } 

        table, #overviewDetailsModal table, #groupDetailsModal table, #analyticsTable { width: 100%; border-collapse: collapse !important; font-family: Arial, sans-serif; margin: 0; }
        table th, table td { border: 1px solid #000 !important; padding: 3px 6px !important; font-size: 14px !important; color: #000; line-height: 1.2; }
        table th { font-weight: bold; background-color: #ffffff; text-align: center; position: sticky; top: 0; z-index: 10; }
        table td:empty::before { content: "-"; }
        table tbody tr:hover { background-color: #f2f2f2; } 

        #printableAreaOverview, #printableAreaGroup, #printWrapper, .modal-body { overflow: auto; max-height: 80vh; padding: 0 !important; }
        #overviewDetailsModal .modal-body, #groupDetailsModal .modal-body { border: 1px solid #000; }
        #groupDetailsModal .modal-content { max-height: 90vh; border: 1px solid #000; margin: 20px auto; width: 95%; display: flex; flex-direction: column; } 

        /* For your Ad Container */
        ins.adsbygoogle { display: block !important; width: 100% !important; min-height: 100px; }
        /* Ad container styling */
        .adsbygoogle { display: block !important; width: 100% !important; min-width: 250px; min-height: 90px; overflow: hidden; } 

        @media print {
            #printableAreaOverview, #printableAreaGroup, .modal-body, #printWrapper { overflow: visible !important; max-height: none !important; border: none !important; }
            body * { visibility: hidden; }
            #printableAreaAnalytics, #printableAreaGroup, #printableAreaOverview { visibility: visible !important; position: absolute !important; left: 0 !important; top: 0 !important; width: 100% !important; margin: 0 !important; }
            #printableAreaAnalytics *, #printableAreaGroup *, #printableAreaOverview * { visibility: visible !important; }
            .print-landscape { width: 297mm !important; }
            @page { size: auto; margin: 10mm; }
            body.landscape-mode { @page { size: A4 landscape; } }
            body.portrait-mode { @page { size: A4 portrait; } }
            #printableAreaAnalytics { transform: scale(0.9); transform-origin: top left; }
            .no-print, .sidebar, .header, .modal-header, button { display: none !important; }
            table th { position: static !important; }
        }
    </style>